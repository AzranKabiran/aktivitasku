<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ─────────────────────────────────── -->

    <!-- Exact alarm (required Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Wake device to fire alarm -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Notifications (required Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Voice input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Vibration for alarm -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Keep app running after reboot -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- ── Application ────────────────────────────────── -->
    <application
        android:name=".AktivitasKuApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="AktivitasKu"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AktivitasKu"
        tools:targetApi="31">

        <!-- Splash / Permission Gate (LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AktivitasKu.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AktivitasKu" />

        <!-- Home Screen Widget -->
        <receiver
            android:name=".presentation.widget.AktivitasKuWidgetReceiver"
            android:exported="true"
            android:label="AktivitasKu Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Alarm BroadcastReceiver -->
        <receiver
            android:name=".service.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aktivitasku.ACTION_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Alarm Action Receiver (snooze / complete from notification) -->
        <receiver
            android:name=".service.AlarmActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aktivitasku.ACTION_SNOOZE" />
                <action android:name="com.aktivitasku.ACTION_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver — reschedule alarms after device restart -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>


        <!-- WorkManager — disable default init so Hilt factory is used -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
